import createCodeLine from '../../../utils/create-code-line';
import { module, test } from 'qunit';

module('Unit | Utility | create code line');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = createCodeLine();
  assert.ok(result);
});
