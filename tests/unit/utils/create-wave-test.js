import createWave from '../../../utils/create-wave';
import { module, test } from 'qunit';

module('Unit | Utility | create wave');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = createWave();
  assert.ok(result);
});
