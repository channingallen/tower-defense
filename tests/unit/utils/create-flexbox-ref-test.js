import createFlexboxRef from '../../../utils/create-flexbox-ref';
import { module, test } from 'qunit';

module('Unit | Utility | create flexbox ref');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = createFlexboxRef();
  assert.ok(result);
});
