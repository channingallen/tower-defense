{{#each groupTowers as |tower|}}
  {{td-game/board/tower-group/tower path=(readonly attrs.path)
                                    select=(action attrs.select-tower tower)
                                    selectedTower=(readonly selectedTower)
                                    tower=(readonly tower)
                                    update-tower-position=(action attrs.update-tower-position)
                                    waveStarted=(readonly attrs.waveStarted)}}
{{/each}}
